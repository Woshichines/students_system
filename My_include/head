#ifndef HEAD
#define HEAD

#include <iostream>
#include <string>
#include <cmath>
#include <list>
#include <array>
#include <vector>
#include <algorithm> // For std::sort, std::find, etc.
#include <fstream>   // For file input/output operations
#include <map>       // For std::map
#include <set>       // For std::set
#include <queue>     // For std::queue
#include <stack>     // For std::stack
#include <tuple>     // For std::tuple
#include <utility>   // For std::pair, std::move, etc.
#include <filesystem>
#include <ctime>
#include <memory>

#include "Students"
#include "Teachers"
#include "Classrooms"

using namespace std;

int 求级数()
{
    long long mytime = time(NULL);
    long long y = 31536000;
    long long year = mytime / y;
    int int_year = static_cast<int>(year);
    return int_year + 1970;
}

template<class InputIterator, class T>
bool find_b (InputIterator first, InputIterator last, const T& val)
{
    for (; first != last; ++first)
        if (*first == val)
            return true;
    return false;
}


class Teachers;
class Students;
class Schools;
class Classrooms;

//a不调用增加，b调用增加

#endif